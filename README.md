🔹 Области хранения данных
База данных (JSON Server) — хранит товары, пользователей, заказы, категории и т.д.

BFF (Backend For Frontend) — управляет сессией текущего пользователя, авторизацией, корзиной.

Redux store — хранит состояние интерфейса: текущий пользователь, список товаров, корзина, заказы.

🔹 Сущности приложения
Пользователь

БД: список пользователей

BFF: сессия текущего пользователя

Redux: отображение профиля и статуса входа

Роль пользователя

БД: список ролей (покупатель, админ, менеджер)

BFF: сессия пользователя с ролью

Redux: использование роли на клиенте (например, доступ к админке)

Товар (product)

БД: список товаров

Redux: отображение каталога и карточек товара

Категория (category)

БД: список категорий (платья, обувь, аксессуары)

Redux: фильтрация и навигация

Заказ (order)

БД: список заказов

BFF: текущая корзина и оформление заказа

Redux: отображение корзины и истории заказов

Отзывы (review)

БД: список отзывов

Redux: отображение отзывов на странице товара

🔹 Таблицы БД
users: id / login / password / registered_at / role_id

roles: id / name

products: id / title / description / price / image_url / category_id / created_at

categories: id / name

orders: id / user_id / created_at / status / total_price

order_items: id / order_id / product_id / quantity / price

reviews: id / product_id / author_id / content / rating / published_at

🔹 Схема состояния на BFF
Сессия текущего пользователя: login / password / role

Корзина: products: [ { product_id, quantity } ]
